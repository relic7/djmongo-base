from django.conf.urls import patterns, include, url
from django.contrib import admin

urlpatterns = patterns('',
    # Examples:
    # url(r'^$', 'md5checks.views.md5index', name='md5index'),
    # url(r'^detail/', 'md5checks.views.md5detail', name='md5detail'),
    # url(r'^update/', 'md5checks.views.md5update', name='md5update'),
    # url(r'^delete/', 'md5checks.views.md5delete', name='md5delete'),

)
